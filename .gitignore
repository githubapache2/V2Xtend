# Firmware build artifacts (idf.py)
firmware/build/
firmware/managed_components/
firmware/dependencies.lock
firmware/sdkconfig.old
firmware/sdkconfig.backup*

# iOS app source is developed privately and not published in this repo
ios/

# iOS build artifacts (xcodebuild / SwiftPM)
ios/**/build/
ios/**/build-sim/
ios/**/.build/
ios/**/DerivedData/
ios/**/xcuserdata/
*.xcuserstate

# Local iOS Xcode backups / scratch (signing, private sources)
ios-m3-backup-*/

# Local test harnesses / PR scratchpads (kept out of the public tree for now)
scripts/
pr_drafts/

# Python venv / pycache / PyInstaller
bridge/__pycache__/
bridge/build/
bridge/dist/
bridge/*.egg-info/
bridge/config/

# Android / Gradle (root is repo root since KMP M1)
.gradle/
build/
local.properties
shared/build/
shared/.gradle/
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/
android/.kotlin/
*.iml
.idea/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/

# Captured frame logs / field pcaps (local)
*.itsg5
*.pcap

# On-device USB diagnostics dumps
blelog.txt
*.key
*.pem

# KMQTT OpenSSL klibs (scripts/fetch_kmqtt_openssl.sh) — ~6 MB each, not in git
openssl-ios-*.klib
openssl-macos-*.klib
openssl-linux-*.klib
openssl-mingw-*.klib
openssl-tvos-*.klib
openssl-watchos-*.klib

website/

# Enthält Klartext-SSH-Passwort für den RPi, siehe Sicherheitshinweis darin
CLAUDE.md
oldCLAUDE.md

# Claude Code / SpecStory local tooling state — not project source,
# .specstory in particular contains full session transcripts (which quote
# CLAUDE.md verbatim, incl. the RPi password above)
.claude/
.specstory/
