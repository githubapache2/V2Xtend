<?xml version="1.0" encoding="utf-8"?>
<!-- Source-row container (Punkt 4b). Theme-attr fill/border (not the
     dark-only literal hexes this used before) so it reads as a light card
     in day theme and a raised dark card in night theme, matching the
     c-its counter chips' theme-aware container approach. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurfaceVariant"/>
    <corners android:radius="12dp"/>
    <stroke android:width="1dp" android:color="?attr/colorOutline"/>
</shape>
